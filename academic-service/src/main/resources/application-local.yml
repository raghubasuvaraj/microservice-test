
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost

server:
  port: 8008

spring:
  datasource:
    url: jdbc:mysql://13.233.254.229/dc
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: dc_db_user
    password: FRLpU-w8lV&a$J)N

  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  logging:
    level:
      root: INFO